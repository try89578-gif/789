--[[
    Title: 999ตัวตึง | ONE BUTTON + RGB + Anti-Idle
    Author: 999ตัวตึง (RGB Glow + Safe Anti-Idle)
]]

-- Load WindUI Library (Ensure the library is included in your environment)
local Wind = require(game:GetService("ReplicatedStorage").WindUI)
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local hue = 0 -- For RGB effect
local isProcessing = false -- Status flag
local baseTypeSpeed = 0.08 -- Base typing speed configuration

-- Create Main Window
local mainWindow = Wind:CreateWindow({
    Name = "999 RGB Utility",
    Theme = "Glass", -- Liquid glass theme
    Glow = true,
    Rounded = true,
})

-- Add Notification Style Frame
local frame = mainWindow:AddFrame({
    Title = "Notification",
    Size = UDim2.new(0, 340, 0, 300),
    Padding = 10,
})

-- Add RGB Effect Button for One-Click Auto Functionality
local button = frame:AddButton({
    Text = "🚀 ONE CLICK AUTO",
    Callback = function()
        if isProcessing then return end
        isProcessing = true
        button:SetText("🔄 Processing...")

        -- Simulate Typing Functionality (Dummy Example)
        wait(2) -- Example processing time
        button:PulseColor(Color3.new(0, 1, 0)) -- Success pulse effect
        button:SetText("✅ Done!")

        wait(1.5) -- Reset state for reuse
        button:SetText("🚀 ONE CLICK AUTO")
        isProcessing = false
    end,
    Draggable = true, -- Make it draggable on mobile and PC
    Style = {
        BackgroundColor = Color3.fromHSV(hue / 360, 1, 1), -- Use the RGB effect on background
        TextColor = Color3.new(1, 1, 1),
        Neon = true,
    },
})

-- Add RGB Hue Loop for Button
RunService.Heartbeat:Connect(function(delta)
    hue = (hue + delta * 50) % 360
    button:UpdateStyle({
        BackgroundColor = Color3.fromHSV(hue / 360, 1, 1)
    })
end)

-- Add Button to Create New Draggable Buttons Dynamically
frame:AddButton({
    Text = "➕ Create RGB Button",
    Callback = function()
        local newButton = frame:AddButton({
            Text = "Custom Button",
            Callback = function()
                print("Custom button clicked!")
            end,
            Draggable = true,
            Style = {
                BackgroundColor = Color3.fromHSV(math.random(), 1, 1),
                TextColor = Color3.new(1, 1, 1),
                Neon = true,
            },
        })
    end,
})

-- Load and Show Window
mainWindow:Show()